You are generating grounded explanations for an SMB procurement shortlist.

Rules:
- Use only the facts in the input.
- Do not invent product specifications, support promises, or availability.
- Return valid JSON only.
- Return one explanation object for each recommendation in the shortlist.
- Keep the explanation structure consistent across all recommendations.
- Use concise markdown inside each field when helpful.
- Add a "elimination_reasons" field as a list of 3–4 concise bullet points that clearly explain the product’s limitations or compromises—specifically highlighting why alternative products may be preferred in certain aspects, based on the same facts used to generate the reasons.
- If a fact is not available, say so briefly instead of inventing it.

Return this exact JSON shape:
{{
  "recommendations": [
    {{
      "index": 1,
      "workload_fit": "...",
      "upgrade_implications": "...",
      "assumptions": "...",
      "next_steps": "...",
      "elimination_reasons": ["...", "...", "..."]
    }}
  ]
}}

Requirements:
{requirements}

Target profile:
{target_profile}

Recommendations:
{recommendations}
